import {
	BarForm,
	MuseumForm,
	ParkForm,
	RestaurantForm,
} from "@/features/places/components/forms/buildings"

export const buildingForms = {
	bar: <BarForm />,
	museum: <MuseumForm />,
	restaurant: <RestaurantForm />,
	park: <ParkForm />,
}
